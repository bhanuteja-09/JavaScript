// String Methods

// split()

var sample = "This is a string sample text"

document.write(sample.split(" "))
console.log(sample.split(" ")) //It will separate the string if you give spaces in slipt(" ")

// trim() (This Method removes the white space around it)

var student = "     ramesh    ";

document.write(student)

console.log(student.trim())

//chatAt()

var student = "ramesh";

document.write(student.charAt(5))
document.write(student.charAt(6)) // not displaying anything because nothing is there in 6th position