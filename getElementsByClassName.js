var myRevenue = document.getElementsByClassName("revenue")[0]

myRevenue.textContent = "Growth"

console.log(myRevenue)

// Note: We should specify the exact position of the ClassName 
// if you have number of ClassNameS with same ClassName then only 
// it will change the "textContent ="Growth" " 

// For that we use [1] or [1] or [3] , to knowing the
// which position of ClassName we should change, see the console.log